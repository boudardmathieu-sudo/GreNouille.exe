{
  "version": 2,
  "buildCommand": "npm run build",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "server.ts": {
      "includeFiles": "dist/**,server/**"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}
